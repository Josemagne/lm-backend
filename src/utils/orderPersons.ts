/**
 * Orders the person objects given a property
 * @param persons Array of person objects
 * @returns 
 */
const orderPersons = (persons: Person[], order: "asc" | "desc"): Person[] => {
    let result: Person[] = []


    return result;
}

export default orderPersons;